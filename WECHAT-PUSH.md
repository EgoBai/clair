# D1 微信推送方案 — 决策锁定（2026-08-13）

## 决策结论
**仅采用 WorkBuddy 自有渠道实现推送，不接入任何第三方推送服务。**

- 不接入：Server酱、企业微信机器人、推送加、Bark、飞书/钉钉 webhook、极光/个推等第三方推送。
- 用户无企业微信；第三方推送引入外部依赖、密钥管理与合规风险，与「零外部依赖、数据自主」原则冲突。

## 自有渠道机制
WorkBuddy 提供项目消息能力 `mcp__wb-issues__project_message_add_or_reply`：
- 在 Clair 项目下发布消息（可 @提及用户）。
- 触发 WorkBuddy 站内通知，并推送至**移动客户端 / 小程序**通知栏。
- 零密钥、零外部网络出口，天然契合 WorkBuddy 运行环境。

## 落地架构（设计，非本次实现）
```
Clair 后端事件（价格预警/大宗异动/AI 信号/日报）
        │  (本地事件总线 / 文件系统标记 / 轻量 HTTP 钩子)
        ▼
WorkBuddy 侧消费者（自动化编排 或 主理人 agent）
        │  调用 mcp__wb-issues__project_message_add_or_reply
        ▼
WorkBuddy 项目消息  ──@提及用户──►  移动端/小程序推送
```
- 推送的"触发"发生在 WorkBuddy 侧（agent/自动化可直接调用上述 MCP 工具），而非 Clair 后端自行调外部 API——后端只负责产生事件与结构化载荷。
- 推送内容须标注 `dataSource` 与生成时间，沿用诚实数据红线（不推送编造行情）。

## 待拍板/待办
- [ ] 确认 Clair 后端→WorkBuddy 消费者的事件传输方式（推荐：后端写 `data/push-events/*.jsonl`，由 WorkBuddy 自动化定时消费并调用项目消息 API）。
- [ ] 定义首批推送场景优先级（价格预警 > 大宗交易异动 > 每日 AI 简报 > 因子轮动信号）。
- [ ] 与 D2 小程序订阅消息打通：小程序 POC 阶段一并设计"订阅消息 + 站内通知"双通道。

## 状态
- 决策：✅ 已锁定（仅自有渠道）。
- 实现：⏳ 待 POC 阶段落地（不在本批次代码改动范围）。
