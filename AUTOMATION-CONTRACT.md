# 自动化任务回写契约（Automation Write-Back Contract）

> 目的：解决「三个每日自动化各自在独立空间文件夹跑、产出不汇聚、我每天不知道它们干了什么」这一痛点。
> 版本：v1 · 建立于 2026-09-08 · 维护者：ARCH Lane
> 适用范围：所有在项目下运行的定时/自动化任务（每日工作总结、每日自主推进循环、每日测评蜂群，以及后续新增的任何自动化）

---

## 一、问题诊断

| 现象 | 根因 |
|------|------|
| 三个自动化各建独立空间文件夹 | 每个自动化是独立沙箱，**本地文件系统不共享** —— 写文件必然散落 |
| 产出无法汇聚 | 没有约定「产出必须写到哪里」，各写各的 |
| 我不知道每天做了什么 | 缺少统一、按日、可扫读的汇总视图 |
| 问题遗留多日才发现 | 没有「无产出也要报」的强制要求，空转时静默 |

**结论**：不能靠文件系统做汇聚，必须走**项目级共享总线**。

---

## 二、总线架构

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ 每日工作总结  │  │ 每日自主推进  │  │ 每日测评蜂群  │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                  │                  │
       │  统一结果卡（第三章格式）            │
       └──────────────┬───┴──────────────────┘
                      ▼
        【总线 1】项目留言板（wb-issues project_message）★ 主总线
                 标题前缀：【自动·XXX】YYYY-MM-DD
                      │
                      ▼
        【总线 2】每日价值汇总官（automation，21:00 每日）
                 扫描当日三条前缀 → 聚合 → 产出【日报】YYYY-MM-DD
                      │
                      ├──▶ 项目留言板（人可读，用户每日只看这一条）
                      └──▶ 看板 todo（次日待办自动建卡，可追踪）
                      │
        【总线 3】项目网盘 /automation-outbox/YYYY-MM-DD-*.md（归档，可选）
```

**为什么主总线是项目留言板**：它是项目级、跨会话、跨沙箱持久、且用户直接可见的唯一通道。自动化任务只需调用 `project_message_add_or_reply` 工具即可写入，不依赖任何本地路径。

---

## 三、结果卡格式（强制）

每个自动化任务**每次运行结束前必须**调用 `project_message_add_or_reply` 发一条**顶层留言**，标题严格遵守前缀约定：

| 任务 | 标题格式 |
|------|----------|
| 每日工作总结 | `【自动·工作总结】YYYY-MM-DD` |
| 每日自主推进循环 | `【自动·自主推进】YYYY-MM-DD` |
| 每日测评蜂群 | `【自动·测评蜂群】YYYY-MM-DD` |
| （汇总官产出） | `【日报】YYYY-MM-DD` |

正文**必须**包含以下 6 段，缺段视为违规：

```markdown
## 1. 今天做了什么
- 动作 1（一句话，动宾结构）
- 动作 2

## 2. 产生了什么价值 / 改变了什么
| 变更 | 证据（commit hash / 端点 URL / 测试数字 / 截图） | 影响面 |
|------|---------------------------------------------|--------|
| xxx  | `abc1234` / https://... / 136/136 pass       | 线上/本地/仅文档 |

## 3. 发现的问题 / 风险
- 【级别 P0/P1/P2】问题描述 + 证据

## 4. 未完成 / 被阻塞
- 事项 + 阻塞原因 + 需要谁解

## 5. 明日建议动作（≤3 条，按优先级）
1. ...
2. ...

## 6. 自检
- 是否真的产出了可验证结果：是 / 否（**否 时必须写明「本期空转」及原因**）
- 是否触碰数据红线（虚构数据/硬编码/空壳）：是 / 否
```

**红线**：
1. **禁止只写本地文件作为唯一产出** —— 本地文件在独立空间里等于不存在。
2. **禁止静默空转** —— 没产出就明写「本期空转 + 原因」，这本身就是最有价值的信号。
3. **禁止无证据的价值声明** —— 每条价值必须挂 commit hash / URL / 数字。
4. **禁止虚构** —— 跑不动就报「未完成」，不粉饰。

---

## 四、各任务职责边界

| 任务 | 它回答的问题 | 它不该做 |
|------|-------------|---------|
| 每日工作总结 | 昨天到今天，人和 Agent 在这个项目上**累计推进了什么**，整体健康度如何 | 不该重复列代码细节 |
| 每日自主推进循环 | 今天**主动**做了哪些改进，改了哪些文件，验证结果如何 | 不该只做调研不落地 |
| 每日测评蜂群 | 线上/本地产品**实际可用吗**，哪些功能坏了、数据对不对 | 不该只报「通过」，必须给出失败项与复现路径 |
| 每日价值汇总官 | 上面三家说了什么，**今天净增了什么价值**，明天该干什么 | 不该自己造数据，只做聚合 |

---

## 五、落地步骤（给项目所有者）

1. 打开三个自动化任务的编辑界面，**把 `AUTOMATION-PROMPT-PATCH.md` 中对应那一段完整粘贴到原 prompt 末尾**（不改原有逻辑，只追加回写要求）。
2. 确认三个任务的运行时段都早于 **21:00**（汇总官在 21:00 聚合）。
3. 「每日价值汇总官」已由 ARCH Lane 创建（automation），无需手动建，但需确认其状态为 ACTIVE。
4. 从第二天起，每天只看一条 `【日报】YYYY-MM-DD` 即可。

---

## 六、验收标准

- [ ] 连续 3 天，留言板出现 3 条 `【自动·XXX】` + 1 条 `【日报】`
- [ ] 任意一条结果卡的「价值」栏都能点开验证（有 hash/URL/数字）
- [ ] 出现空转时，`【日报】` 中明确标注「本期空转」而不是被静默跳过
- [ ] 用户能仅凭 `【日报】` 一条消息，说出今天做了什么、有什么价值、明天干什么
