# 2026-09-06 工作日志

## 第112轮（14:30）· 自主循环 IP-14 组合鉴权收口

- **任务**：QA 蜂群 IP-14（P0 安全）——关闭游客未登录直读 `/api/portfolio` 默认组合（¥27万成本/浮亏/现金）的越权信息泄露。
- **实装**：`backend/src/api/portfolio.ts` 加 `router.use('/portfolio', authMiddleware)`（+2 行），对齐 `watchlist.ts:20` 401 口径；risk-center 经 `getDefaultPortfolio()` 函数直连不受影响。
- **验证**：supertest 反例探针 3/3（无 token→401 / 带 token→放行 / 无 token POST→401，探针用后即删）；后端 tsc 0 新增错；前端 tsc 0错 / build 9.33s / e2e route-render-smoke 64/64。
- **红线**：目标文件 portfolio.ts 与在途 {lockup-shares.ts(活跃), NorthBoundPage.tsx(陈旧)} 零交集（D22 二级判定合法推进）；未碰任何在途文件。
- **待用户**：D22 红线二级判定追认 / 收口活跃在途 lockup-shares.ts（解锁 IP-12）/ D21-A NorthBoundPage 收口 / D24 龙虎榜死链。

> 注：后端进程（PID 24792，`tsx src/index.ts` 无 watch）需重启方能加载新鉴权代码；自动化未擅自重启用户托管服务，验证经直接模块集成测试完成。

## 第113轮（20:42）· 自主循环 IP-15 backtest 信号引擎归零攻坚

- **任务**：QA 蜂群 IP-15（P1·连续 11 日最痛项）——backtest 实接口返回 `totalTrades:0`（静默归零）。
- **根因**：默认初始本金 `initialCapital || 100000` 在高价股（茅台 ~1474/股）下 `buyAmount = floor(100000/(1474*100))*100 = 0` → 永不买入 → 三类策略 totalTrades 全 0。
- **实装**：`backend/src/utils/backtestEngine.ts` 默认本金 100000→1000000（行 317）+ `BacktestResult.warnings:string[]`（行 96）+ 资金不足告警（行 379/446）+ `STRATEGY_PRESETS` 五条预设同步 1000000；`backtest.test.ts` 改 `toBe(1000000)` + 新增 IP-15 高价股回归测试（杜绝静默归零）。
- **验证**：重启后端（PID 24792→88789）实接口 600519 → `ma_cross=4 / rsi_reversal=2 / macd_trend=2` 真实成交（修复前全 0）；vitest 61/61 / 前端 tsc 0错 / build 8.35s / 后端 tsc 仅既有 `ai-analysis.ts(89,5)` 基线错；pg 直连复算 MA(5,20) 金叉=4/死叉=4 证信号层正常。
- **红线**：目标 backtestEngine.ts 与在途 {lockup-shares.ts(活跃), NorthBoundPage.tsx(陈旧)} 零交集（D22 合法推进）；未碰任何在途文件。
- **待用户**：D22 红线二级判定追认 / 收口活跃在途 lockup-shares.ts（解锁 IP-12）/ D21-A NorthBoundPage 收口 / D24 龙虎榜死链 / 清理预存 tsc 错 ai-analysis.ts(89,5)。
