# 2026-08-10 工作日志

## D14 真实数据源全面接入（A + B 落地，已 push f7f7539a7）

用户指令：「先把稳定可靠的真实数据源全面接入，倾向 A 和 B」。

- **探查结论**：
  - 后端资金流实际主真实源是**东方财富免 key 直连**（个股/行业正常路径真实），此前误判"Tushare 积分阻塞资金流"——路由根本没调 Tushare provider 链，纠正之。
  - 验证东财个股/行业/北向接口真实可达（curl + node 清空代理）。
  - 腾讯自选股(westock)/通达信(tdx) MCP 已连（免 key），但后端 Node 无法直接 import MCP。

- **A（行业兜底改诚实空态）**：`fund-flow.ts` 行业路由东财失败分支，原 `demo*2.5` 编造 → 改为返回 `industries:[]` + `source:'unavailable'` + note「后端未接入」。消除诚实红线瑕疵。

- **B（北向真实化 + 离岸诚实空）**：`fundFlowProviders.ts` 重写 `getGlobalIndicators`，新增 `fetchNorthBoundReal()`（东财 kamt，读 sh2hk+sz2hk 北向真实）；离岸人民币无 AlphaVantage key 时诚实空（`dataSource:'unavailable'`），不再 demo。修了一处字段 bug（原误读 hk2sh/hk2sz 南向→0）。

- **验证**：esbuild bundle + node 真实调用 → `dataSource:'real'`、`north_bound` latest=0.04 亿（真实）；fundFlow 7 文件 155 测试全绿。

- **DECISION_LOG D14** → ✅ 已解决（真实源全面接入，Tushare/AlphaVantage 降级可选）。

- 提交 `f7f7539a7` 已 push origin/main。automation 保持 PAUSED，其 08-04~08-07 在途文件未提交、未碰。

## 待续

- POC 四件套（小程序迁移）、微信推送渠道评估、多 agent 蜂群分工（依据 clair后续执行计划_20260731.md）。
- 离岸人民币真实源若需补全：东财/新浪外汇接口待稳定方案（当前诚实空）。

## T2 市场宽度游客公开 + 真实源替换模拟（commit 3e34ae5f7，本地未 push）

用户 2026-08-10 选「各页真实数据收尾」首做 breadth，并选「对游客公开」。

- **两个隐藏 bug（非 auth 本身）**：
  1. `breadthRouter` 挂载在 `/api`（app.ts），但其路由定义是 `/current`/`/sectors`（无 `/breadth` 前缀）→ Express 只剥掉 `/api`，实际路径变成 `/api/current`，与前端 `breadthService`(`/breadth/current`) 及文档(app.ts:366)不符。改挂 `/api/breadth` 修复。
  2. `watchlist.ts` 用 `router.use(authMiddleware)`（无路径）做全路由鉴权；watchlistRouter 挂在 `/api` 且位于 breadth 等 30+ 路由**之前** → Express 把每个 `/api/*` 都灌入 watchlistRouter，blanket auth 把未匹配 watchlist 路由的请求（含 `/api/breadth/*`）统一 401。改为 `router.use('/watchlist', authMiddleware)` 收敛范围（F05 私有权限仍守住）。
  - 排查手法：先确认 authMiddleware 仅出现在 watchlist + logout；再用实际 curl 对比「watchlist 前(P200)/后(P404)」路由定位是 watchlist 阻断；最后用 `/api/current`→200 反证 breadth 路由本体正常、只是前缀错位。

- **诚实数据红线**：`marketBreadth` 原 `Math.random` 全量模拟 → 游客公开会暴露假数据，违反红线。改为接 `realMarketData.fetchBreadth`（东财 push2 真实涨跌家数/成交额/量能比）；源缺失（沙箱无 egress）时诚实空 `dataSource:'unavailable'`。`realMarketData.fetchBreadth` 增补 upVolume/downVolume/volumeRatio 真实字段。板块宽度/历史时序真实源未接入 → 返回空（不实随机）。

- **验证**：`/api/breadth/current` 游客免登录 200（诚实空）；`/api/watchlist` 仍 401；marketBreadth 单测 10/10 绿。前端 5173 / 后端 3001 均 200。

- **待 push**：GitHub 仍不可达（网络故障），3e34ae5f7 及此前 4 个本地提交待推送。

- **剩余 T 票**：T1b 前端首页指数卡接 /api/market/realtime；T3-T7 ETF/港股通/研报/财务/因子真实化。breadth 板块宽度+历史时序真实源待接入（诚实空占位）。
