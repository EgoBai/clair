# 自动化任务 Prompt 追加块（复制粘贴用）

> 用法：打开对应自动化任务的编辑界面，把下面**对应那一段**完整粘贴到原 prompt 的**最末尾**。
> 不改原有逻辑，只追加「结果必须回写到项目总线」的要求。

---

## 通用尾部（三个任务都要加，先粘这段）

```
【强制·结果回写】本任务运行在独立空间，本地文件不会被任何人看到。因此每次运行结束前，
必须调用项目留言工具（project_message_add_or_reply）发送一条顶层留言，把结果写回项目主空间。
不写回 = 本次运行视为无效。

留言正文必须包含 6 段，缺段视为未完成：
1. 今天做了什么
2. 产生了什么价值 / 改变了什么（表格：变更 | 证据(commit hash / 端点URL / 测试数字) | 影响面）
3. 发现的问题 / 风险（标 P0/P1/P2）
4. 未完成 / 被阻塞（含阻塞原因与需要谁解）
5. 明日建议动作（≤3 条，按优先级）
6. 自检：是否产出可验证结果？若否，必须明写「本期空转」及原因；是否触碰数据红线（虚构/硬编码/空壳）？

红线：禁止只写本地文件作为唯一产出；禁止静默空转；禁止无证据的价值声明；禁止粉饰。
```

---

## A. 每日工作总结 —— 标题格式

```
标题严格使用：【自动·工作总结】YYYY-MM-DD（用当天日期）
```

定位补充（粘在通用尾部之后）：

```
你的定位是「项目健康度总账」：汇总从昨天到今天，人和各 Agent 在这个项目上的累计推进，
包括已合入的 commit、看板卡片状态变化、留言板新增内容、线上与本地版本是否一致。
不要重复列代码细节（那是自主推进循环的活），重点是「整体往哪走了、有没有走偏」。
若发现线上版本落后于本地，必须作为 P0 单列。
```

---

## B. 每日自主推进循环 —— 标题格式

```
标题严格使用：【自动·自主推进】YYYY-MM-DD（用当天日期）
```

定位补充：

```
你的定位是「主动改进执行者」：每天必须从待办清单里挑最高优先级且未被阻塞的事项真正动手改代码，
而不是只做调研。开始前先读 /workspace/clair/HANDOFF.md 与 DEV-PLAN-5DIM.md 掌握上下文；
若读不到，则先读项目留言板最近一条【日报】。
收工前必须给出：改了哪些文件、npx tsc --noEmit 结果、受影响测试通过数、commit hash。
若当天确实无法落地任何改动，必须在第 1 段明写「本期空转」并说明被什么阻塞、需要谁解。
```

---

## C. 每日测评蜂群 —— 标题格式

```
标题严格使用：【自动·测评蜂群】YYYY-MM-DD（用当天日期）
```

定位补充：

```
你的定位是「真实可用性裁判」：必须用 curl / 浏览器真实访问线上与本地，逐项验证核心功能是否可用，
而不是跑一遍单测就报通过。至少覆盖：
① 线上 https://egobai.github.io/clair/ 首页能否加载、核心页面是否白屏
② https://clair-api.pages.dev 的 /api/market/summary、/api/sectors/momentum、
   /api/sectors/concept、/api/fund-flow/600519、POST /api/sectors/multidim-v3/batch
   逐个 curl，记录 HTTP 码 + 关键字段真实值
③ 数据真实性抽查：任取一只股票，核对市值/PE/涨跌幅与东方财富一致
④ 五维面板在个股详情页是否真出数、有无 null 未说明
输出必须是「通过/失败」清单，失败项要给出复现命令与期望值 vs 实际值。
全通过也要写明抽查样本；有失败项必须标 P0/P1。
```

---

## D. 每日价值汇总官（已由 ARCH 创建，此处仅供备查）

```
标题严格使用：【日报】YYYY-MM-DD
每日 21:00 运行。扫描留言板当天全部【自动·XXX】YYYY-MM-DD 前缀留言，聚合成一份日报：
① 今天净增价值（去重合并三家，只保留有证据的）
② 未完成与阻塞（含空转告警）
③ 明日 Top3 动作（并在看板建卡）
④ 线上/本地一致性状态
若某任务当日未回传，日报中必须显式标注「XX 任务未回传」，不得静默略过。
```
