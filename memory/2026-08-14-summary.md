# Clair 每日总结 — 2026-08-14

## 今日提交

**1 个提交** — 修复 DiscoverPage AI 市场解读不生效问题。

## 修复内容

| 模块 | 变更 |
|------|------|
| `backend/src/api/ai-chat.ts` | 提取 `buildRuleInsight()` 公共函数；后端 `/api/ai/market-insight` 补充 `summary`/`points`/`metrics` 前端契约字段；`/api/ai/market-insight-llm` LLM 失败时降级为规则引擎结果 |
| `frontend/src/pages/DiscoverPage.tsx` | 修复 AI 解读区错误处理：`r.ok` 检查 + `finally` 关闭 loading + 失败时显示降级提示 |
| `frontend/vite.config.ts` | 代理 timeout 从 15s 调为 60s，避免 LLM 慢请求被代理掐断 |
| `clair-worker/worker.js` | 生产环境同步：`generateMarketInsight()` 补充契约字段；新增 `/api/market/summary` 路由；新增 `/api/ai/market-insight-llm` 路由返回规则引擎结果 |

## 验证结果

- 后端 `tsc --noEmit`：0 个 `ai-chat.ts` 错误（其余 2 个既有错误与本次无关）
- 前端 `tsc --noEmit`：0 错误
- curl + 浏览器端到端验证：AI 市场解读区正常显示 summary、5 条 points、6 项 metrics
- 截图确认页面渲染正常

## 已知外部依赖

- DeepSeek API Key 当前余额不足（`Insufficient Balance`），`/api/ai/market-insight-llm` 会降级返回规则引擎结果，用户仍能看到完整 AI 解读内容。待充值后可自动恢复 LLM 增强。
