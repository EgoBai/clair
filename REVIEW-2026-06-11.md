# 澄观 Clair — 阶段性复盘与研发计划

> 2026-06-11 | ego_bai + Hermes

## 一、现状复盘

### 做得好的 ✅
- 核心架构完整：4页闭环（发掘→筛选→自选→复盘）
- AI后端9个端点已开发
- TypeScript 0错误，构建通过
- 页面清理（30→10），代码瘦身
- 性能优化（缓存/索引/懒加载）

### 关键缺口 ❌（用户实测反馈）

**1. FloatingChat AI对话不生效**
- 根因：Vite proxy指向 clair-api.pages.dev（生产），而非本地 localhost:3001
- chatStream用SSE流式，生产端可能未正确配置DeepSeek
- 修复：切换proxy到本地 或 确认生产端AI可用

**2. 筛选页功能未跑通**
- 根因：筛选依赖stocks数据，数据来源/api/stocks?limit=200通过proxy到生产端
- 本地后端有PostgreSQL(5541只)，但前端不走本地
- 策略模板的filter函数依赖price/changePercent等字段，数据结构可能不匹配
- 修复：确保数据流从本地后端→前端完整贯通

**3. 自选股搜索不到**
- 根因：/api/search通过proxy到生产端
- 本地PostgreSQL有完整数据，但前端不走本地
- 修复：Vite proxy指向本地后端

**核心根因：前端开发时proxy指向生产端(clair-api.pages.dev)，而非本地后端(localhost:3001)
导致所有数据/AI功能都依赖生产端，而生产端可能未部署最新代码或未配置API Key**

### 自我审查
- 问题：Phase 5-12标记"完成"但实际功能未端到端验证
- 原因：只验证了TypeScript编译和构建，未验证运行时功能
- 教训：**"编译通过"≠"功能可用"，必须端到端验证**

---

## 二、研发计划（两阶段）

### 第一阶段：基础跑通（目标：所有基础功能端到端可用）

**P0 — 立即修复（今天）**
1. Vite proxy切换到本地后端 localhost:3001
2. 验证本地后端AI功能（DeepSeek API调用）
3. 验证股票搜索功能（/api/search本地可用）
4. 验证筛选页数据加载+筛选逻辑

**P1 — 数据基础（本周）**
5. 确认PostgreSQL数据完整性（5541只A股）
6. 行情同步服务稳定运行
7. 批量行情接口可用（/api/stocks/batch/quotes）
8. 自选股CRUD端到端验证

**P2 — AI基础（本周）**
9. AI对话（chatStream）端到端可用
10. AI诊断（/api/ai/diagnose）返回有效结果
11. AI市场洞察（/api/ai/market-insight）可用
12. AI选股推荐（筛选页）返回有效建议

**P3 — 体验基础（下周）**
13. 四页核心循环端到端走通
14. 移动端基本可用
15. 错误处理和加载状态
16. 性能指标达标（LCP<2.5s）

### 第二阶段：AI体验创新（第一阶段完成后）

**P4 — AI陪伴深化**
17. FloatingChat智能对话（上下文记忆、快捷指令）
18. AI诊断报告增强（技术面+基本面+资金面）
19. AI策略回测（自然语言→策略→回测结果）
20. AI异动提醒（自选股异动→AI解读）

**P5 — 交互创新**
21. 对话式筛选（自然语言→筛选条件）
22. AI陪伴引导（新手引导、市场解读）
23. 智能复盘（AI分析交易行为）
24. 个性化推荐（基于用户行为）

**P6 — 差异化体验**
25. 与传统投资网站完全不同的交互范式
26. AI-first的信息架构
27. 情感化设计（不只是数据，是陪伴）

---

## 三、自省机制

### 每次提交后的自检清单
1. 功能是否端到端可用（不只是编译通过）
2. 数据是否真实有效（不只是Mock）
3. 用户体验是否完整（不只是功能存在）
4. 是否符合产品哲学（AI陪伴>冷数据终端）

### 每日复盘
- 今天完成了什么
- 发现了什么问题
- 明天优先做什么
- 产品方向是否对齐

### 关键原则
- **"能用"比"做完"重要**
- **数据真实性是底线**
- **AI是核心差异化，不是装饰**
- **做减法，聚焦核心价值**
